# Keep the build context (and image) small and secret-free.
.git
.gitignore
.dockerignore
Dockerfile
docker-compose.yml
README.md
LICENSE
assets/

# Secrets and local config — provided at runtime, never baked in.
.env
config.yaml

# Generated output
proposals/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# macOS
.DS_Store
